process {
    withName: '.*REPROCESS10X_VALIDATELOCAL' {
        queue         = 'normal'
        ext.args      = params.no_exit_local ? "--no-exit" : ""
        errorStrategy = "finish"
        cpus          = 1
        memory        = 2.GB
    }
}
