process {
    withName: '.*FETCH10XMETA' {
        queue      = 'transfer'
        maxForks   = 10
        cpus       = 1
        array      = 100
        memory     = { 4.GB + 2.GB * (task.attempt - 1) }
    }
}