process {
    withName: '.*REPROCESS10X_AGGREGATEMETA' {
        ext.args = "--qc-suffix ${params.qc_suffix}"
        queue  = 'normal'
        cpus   = 1
        memory = 2.GB
        array  = 100
    }
}
