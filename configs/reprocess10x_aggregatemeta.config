process {
    withName: '.*REPROCESS10X_AGGREGATEMETA' {
        ext.args = params.qc_suffix ? "--qc-suffix ${params.qc_suffix}" : '--qc-suffix solo_qc'
        queue  = 'normal'
        cpus   = 1
        memory = 2.GB
        array  = 100
    }
}
