process {
    withName: 'REPROCESS10X_MAPPINGQC' {
        queue = 'normal'
        cpus  = 1
        memory = '2 GB'
        array = 500
        publishDir = [
            path: { "${params.output_dir}/$prefix/metadata" },
            mode: params.publish_mode,
        ]
    }
}