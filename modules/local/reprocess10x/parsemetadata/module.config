process {
    withName: 'REPROCESS10X_PARSEMETADATA' {
        queue = 'transfer'
        cpus  = 1
        memory = '2 GB'
        publishDir = [
            path: { "${params.output_dir}/$prefix/metadata" },
            mode: params.publish_mode,
        ]
    }
}